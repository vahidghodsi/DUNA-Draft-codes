import { css } from "@emotion/react";

const styles = css({
  display: "flex",
  alignItems: "center",
  height: "100%",
  "._title": {
    marginTop: "0 !important" ,
    marginBottom: "0 !important",
  },
});

export default styles;
