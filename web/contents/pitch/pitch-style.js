import { css } from "@emotion/react";

const styles = css({
  "._container": {
    ".form-container": {
      height: "100%",
      display: "flex",
      // justifyContent: "center",
      alignItems: "center",
      "._FormMultiStep": {
        flexGrow: "1",
        maxWidth: "700px",
        margin: "0 5vw"
      },
    },
  },
});

export default styles;
